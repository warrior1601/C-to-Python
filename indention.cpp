#include <iostream>

using namespace std;

int main()
{
    int x = 1;

    if(x ==1)
    {
        cout << "x is 1." << endl;
    }
    return 0;
}

// This file shows how python if statemens dffer from c++
// you can still use indention to see your code better but the "if" statment
// is not inside the scope you can clearly see where it begins and ends